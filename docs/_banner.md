> **Heads up**  
> This docs site renders markdown from `/docs` with a custom Next+MDX layout.  
> Update `_sidebar.json` to adjust nav order, and add MDX anywhere—`Callout` is available for quick notes.
